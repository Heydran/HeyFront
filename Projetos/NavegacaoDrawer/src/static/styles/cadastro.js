import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    mainContainer:{
        flex:1,
        backgroundColor: "#555"
    },
    containerCadastro:{
        marginTop:20,
        marginLeft: 50
    },
    inputCadastro:{
        borderWidth: 2,
        borderColor: "#0ff",
        backgroundColor: "#222",
        borderRadius: 5
    }
})

export default styles