import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    mainContainer:{
        flex:1,
        backgroundColor: "#555"
    },
    containerConsulta:{
        marginTop:20,
        marginLeft: 50

    }
})

export default styles