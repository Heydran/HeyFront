import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    mainContainer:{
        flex:1,
        backgroundColor: "#555"
    },
    userCard:{
        borderColor: "#0ff",
        borderWidth: 2,
        marginTop: 2,
        backgroundColor: "#222",
          
    },
    userLine:{
        color: "#0ff"
    }
})

export default styles